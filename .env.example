# =============================================================================
# CodeNomad + OpenCode - Environment Configuration
# =============================================================================
# Copy this file to .env and fill in your values:
#   cp .env.example .env
# =============================================================================

# ---------------------------------------------------------------------------
# CodeNomad Server Authentication
# ---------------------------------------------------------------------------
# Username and password for the CodeNomad web UI.
# These credentials are required to access the browser-based interface.
CODENOMAD_SERVER_USERNAME=admin
CODENOMAD_SERVER_PASSWORD=changeme

# Set to "true" to disable CodeNomad authentication entirely.
# Only use this behind a trusted perimeter (VPN, SSO, firewall, etc).
CODENOMAD_SKIP_AUTH=false

# ---------------------------------------------------------------------------
# CodeNomad Server Settings
# ---------------------------------------------------------------------------
# Host port to expose the CodeNomad web UI on
CODENOMAD_HTTP_PORT=9899

# Allow CodeNomad to browse the full filesystem inside the container
# (default: false, restricted to /workspaces)
CODENOMAD_UNRESTRICTED_ROOT=false

# ---------------------------------------------------------------------------
# OpenCode Server Authentication
# ---------------------------------------------------------------------------
# Password for the OpenCode HTTP server API (used internally by CodeNomad)
# If blank, OpenCode's internal API is unsecured (only accessible within
# the container, so this is generally safe to leave empty).
OPENCODE_SERVER_PASSWORD=
OPENCODE_SERVER_USERNAME=opencode

# ---------------------------------------------------------------------------
# Workspace Path
# ---------------------------------------------------------------------------
# Host path to mount as the workspace directory inside the container.
# This is where your project code lives. OpenCode will read/write here.
WORKSPACES_PATH=./workspaces

# ---------------------------------------------------------------------------
# LLM Provider API Keys
# ---------------------------------------------------------------------------
# Set the API key(s) for the LLM provider(s) you want to use.
# At least one is required for OpenCode to function.

# Anthropic (Claude models)
ANTHROPIC_API_KEY=

# OpenAI (GPT models)
OPENAI_API_KEY=

# OpenCode Zen (curated models from OpenCode team)
OPENCODE_API_KEY=

# OpenRouter (multi-provider gateway)
OPENROUTER_API_KEY=

# ---------------------------------------------------------------------------
# Git Configuration (optional)
# ---------------------------------------------------------------------------
# If you need git operations inside the container, you can set these.
# Alternatively, mount your .gitconfig and .ssh directory (see docker-compose.yml)
# GIT_AUTHOR_NAME=Your Name
# GIT_AUTHOR_EMAIL=your@email.com
# GIT_COMMITTER_NAME=Your Name
# GIT_COMMITTER_EMAIL=your@email.com
